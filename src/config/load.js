/**
 * Load .haitaskrc (single source of truth).
 * Custom loader — no hardcoded defaults beyond file presence.
 */

// Stub
export function loadConfig(configPath) {
  throw new Error('Not implemented');
}
