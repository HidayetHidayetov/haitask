/**
 * haitask init — Create .haitaskrc, validate .env
 * Thin handler: no business logic here, delegate to services.
 */

export async function runInit() {
  // TODO: wire to config init + env validation
  console.log('haitask init — not implemented yet');
}
